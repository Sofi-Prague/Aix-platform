import { IndexWorkspaceShell } from "@/components/IndexWorkspaceShell";

// This is the Week 1 target: the three-panel shell (Tree / Detail / Co-Pilot)
// rendering with no real data — just the layout, empty states, and the
// tablet/mobile collapse behavior from PRD §6.2.
export default function WorkspacePage() {
  return <IndexWorkspaceShell />;
}

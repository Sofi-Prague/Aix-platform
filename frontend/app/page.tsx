export default function Home() {
  return (
    <main style={{ padding: "var(--aix-space-xl)" }}>
      <h1>AIX</h1>
      <p style={{ color: "var(--aix-color-text-muted)" }}>
        Platform shell. Head to <code>/workspace</code> for the Index Builder
        three-panel layout (Week 1 target).
      </p>
    </main>
  );
}
